<div class="w-full">
  <h1 class="font-bold text-4xl mb-4">Welcome to Community Connect</h1>
  <p class="text-lg mb-4">You are signed in as <strong><%= current_user.email %></strong></p>
  <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
</div>
