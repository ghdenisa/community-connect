<nav class="bg-white shadow-sm border-b border-gray-200">
  <div class="container mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <!-- Brand/Logo -->
      <div class="flex-shrink-0">
        <%= link_to @brand, helpers.root_path, class: "text-xl font-bold text-gray-900 hover:text-gray-700" %>
      </div>

      <!-- Navigation Links -->
      <div class="flex items-center space-x-4">
        <span class="text-sm text-gray-600">
          <%= helpers.current_user.email %>
        </span>
        <%= link_to t("components.navbar.edit_profile"), helpers.edit_user_registration_path,
            class: "px-4 py-2 text-sm font-medium text-gray-700 hover:text-gray-900" %>
        <%= button_to t("components.navbar.sign_out"), helpers.destroy_user_session_path, method: :delete,
            class: "px-4 py-2 text-sm font-medium text-white bg-red-600 hover:bg-red-700 rounded-md transition-colors" %>
      </div>
    </div>
  </div>
</nav>
