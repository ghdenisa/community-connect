<nav class="bg-white shadow-sm border-b border-gray-200">
  <div class="container mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <!-- Brand/Logo -->
      <div class="flex-shrink-0">
        <%= link_to @brand, helpers.root_path, class: "text-xl font-bold text-gray-900 hover:text-gray-700" %>
      </div>

      <!-- Navigation Links -->
      <div class="flex items-center space-x-4">
        <span class="text-sm text-gray-600">
          <%= helpers.current_user.email %>
        </span>
        <%= render Button::Component.new(
          text: t("components.header.edit_profile"),
          href: helpers.edit_user_registration_path,
          variant: :ghost
        ) %>
        <%= render Button::Component.new(
          text: t("components.header.sign_out"),
          href: helpers.destroy_user_session_path,
          as: :button_to,
          method: :delete,
          variant: :danger
        ) %>
      </div>
    </div>
  </div>
</nav>
